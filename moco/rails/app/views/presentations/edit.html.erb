<h1>Editing presentation 
</h1>

<%= render 'form' %>

<%= button_to 'Delete', presentation_path, :method => :delete, :confirm => "Are you sure?", :class => 'btn btn-small btn-danger' %>
<%= link_to 'Back', presentations_path, :class => "btn" %>
